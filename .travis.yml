language: elixir
elixir: '1.8'
script:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get
  - mix test
deploy:
  provider: gae
  keyfile: client-secret.json.enc
  project: corded-forge-217101
